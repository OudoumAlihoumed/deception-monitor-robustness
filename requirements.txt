# Core scientific stack (CPU pipeline validation + figures)
numpy>=1.26,<3
scipy>=1.11
scikit-learn>=1.4
joblib>=1.3
pyyaml>=6.0
matplotlib>=3.8

# Optional: Modal remote GPU experiment
modal>=0.64

# Optional: local GPU / HF (also installed inside Modal image)
# torch>=2.2
# transformers>=4.44
# accelerate>=0.33
# peft>=0.12
# datasets>=2.20
# huggingface_hub>=0.24
# tqdm>=4.66
